#!/usr/bin/env bash
# Refresh the public example journal (site/example/, served on GitHub Pages) from your local story pages.
# This is now `ramble share --all`: it asks before pushing, because pushing makes your journal public.
exec ramble share --all "$@"
